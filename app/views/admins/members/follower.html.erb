<h2>Adminフォロー</h2>

<% if @members.count > 0 %>
  <table cellspacing="5">
    <tbody>
    <%= render partial: "admins/members/member", collection: @members  %>
    </tbody>
  </table>
<% else %>
  <p>フォローしている人はいません</p>
<% end %>