<tr>
  <td><%= part.name %><td>
  <td><%= part.updated_at.strftime("%Y年%-m月%-d日(%a) %H:%M") %></td>
  <td><%= link_to "編集", edit_admins_part_path(part) %></td>
  <td><%= render "layouts/status_form", { model: part, action: admins_part_path(part) } %></td>
</tr>