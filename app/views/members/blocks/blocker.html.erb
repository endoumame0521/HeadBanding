<h2>Memberブロックリスト</h2>

<% if @members.count > 0 %>
  <table cellspacing="5">
    <tbody>
    <%= render partial: "members/members/member", collection: @members  %>
    </tbody>
  </table>
<% else %>
  <p>ブロックしている人はいません</p>
<% end %>