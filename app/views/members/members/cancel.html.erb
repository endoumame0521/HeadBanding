<h1>本当に退会しますか？</h1>

<%= form_with model: @member, url: member_withdraw_path(@member), local: true do |f| %>
  <div class="field">
    <%= f.hidden_field :status, value: :disable %>
    <%= f.submit "退会する", class: "actions btn btn-danger" %>
  </div>
<% end %>
