<h2>Memberフォロワー</h2>

<% if @members.count > 0 %>
  <table cellspacing="5">
    <tbody>
    <%= render partial: "members/members/member", collection: @members  %>
    </tbody>
  </table>
<% else %>
  <p>フォロワーはいません</p>
<% end %>