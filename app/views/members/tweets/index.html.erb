<h2>Memberツイート一覧</h2>

<!-- バリデーションのエラーメッセージ -->
<%= render "/layouts/error_messages", model: @tweet %>

<table cellspacing="5">
  <!-- ツイート一覧 -->
  <tbody id="tweets">
    <% @tweets.each do |tweet| %>
      <tr id="tweet-<%= tweet.id %>">
        <%= render partial: "tweet", locals: { tweet: tweet, tweet_flag: true } %>
      </tr>
    <% end %>
  </tbody>
</table>

<!-- ツイート投稿フォーム -->
<%= render "form", { tweet_flag: true } %>